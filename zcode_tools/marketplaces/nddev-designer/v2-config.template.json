{
  "_comment": "Disabled-by-default explicit API-key provider for nddev-designer. Its custom identity never reuses a ZCode-owned builtin:* ID, so restored Z.ai OAuth remains app-managed. Enable it only after supplying ZAI_API_KEY through build/.env; credentials.json is restored runtime state and is never templated.",
  "provider": {
    "custom:zai-api-key": {
      "name": "Z.ai - API Key",
      "kind": "anthropic",
      "options": {
        "apiKey": "${ZAI_API_KEY}",
        "baseURL": "https://api.z.ai/api/anthropic"
      },
      "enabled": false,
      "source": "custom",
      "models": {
        "GLM-5.2": {
          "limit": { "context": 1000000 },
          "modalities": {
            "input": ["text"],
            "output": ["text"]
          }
        }
      }
    }
  }
}
