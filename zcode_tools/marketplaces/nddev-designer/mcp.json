{
  "_comment": "No global MCP servers are shipped by design. The designer profile stays portable and receives project-specific integrations from the active workspace. Any deliberately added user-scope server is merged into cli/config.json under mcp.servers.",
  "mcpServers": {}
}
