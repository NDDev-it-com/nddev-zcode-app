{
  "_comment": "No global hooks are shipped by design. The designer profile stays portable and receives project-specific automation from the active workspace. If a durable user-scope hook is deliberately added later, the installer merges it into cli/config.json under the matching event key."
}
